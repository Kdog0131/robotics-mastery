What is a list and how do you access items in it: A list is a data type you can use to store a list of values, to add to it you need to type the name you gave the list and then use the append keyword. (Example: Distance.append) You can also add an value to a specific index on the list (Example: readings[0] -> gets the first item), and you can acess a specific value in the list. (Example: listname[index]) 

What is a dictionary and how is it different from a list: A dictionary allows you to store values that are tied to a specific key that you give each value. Its different from a list because of how easy it is to modify on the fly since you are given the names of each key.

Why did we use a dictionary for sensor readings instead of a list: Because in that case, we needed a quick way to modify and update each sensor reading so that it constantly updates with new readings.

What did todays program teach me about order in Python: What it has taught me was to keep in mind that python reads the program files from top to bottom and this is important when you want to make changes to your code or update a value in your code.
